[pytest]
testpaths = tests
addopts = -ra
markers =
    unit: isolated deterministic unit tests
    protocol: Telnet/ANSI/protocol parser tests
    integration: tests spanning production module boundaries
    regression: reproductions of previously escaped defects
    qt: Qt startup, bridge, widget, and lifecycle tests
    fuzz: deterministic seeded parser/protocol abuse tests
